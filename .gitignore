# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/

# Trainer artifacts (huge — regenerate locally).
# Exclude contents but keep the README (whitelisting requires that the parent
# directory itself isn't ignored — git won't descend into a fully-ignored dir).
trainer/data/
trainer/inputs/*
!trainer/inputs/README.md

# OS / editor
.DS_Store
Thumbs.db
.cursor/
.vscode/
.idea/

# Logs
*.log

# Node / React
node_modules/
web/node_modules/
dist/
build/
web/dist/
.vite/

# Environment files
.env
.env.*

# Intentionally tracked (do not add ignore rules for these):
# backend/, frontend/, web/src/, web/public/, trainer/, README.md, start.bat,
# backend/models/asl.pt, web/public/assets/reference/